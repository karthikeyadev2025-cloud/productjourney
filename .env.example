# Optional — you can also set these inside the app's Settings tab.
# When present, they become the defaults; values saved in the UI override them.

SUPERADMIN_EMAIL=productjounery@gmail.com
SUPERADMIN_PASSWORD=Karthi@2025
PORT=4400

# Gemini
GEMINI_API_KEY=

# Supabase (new project)
SUPABASE_URL=
SUPABASE_SERVICE_KEY=
